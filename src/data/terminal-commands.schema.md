# terminal-commands.json schema
19 条指令,六分类(navigation/store/bestiary/radar/utilities/misc)。
`command` 指令原文不翻译;`usage` 示例;`effect` 中文释义(对照 Ship 页 §Terminal 指令表逐条改写)。
指令识别规则:不区分大小写、前 3 字母即可识别(评 wiki 原文)。
